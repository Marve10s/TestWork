---
import { Image } from 'astro:assets';
import { voices } from '@data/features';
---

<div class="bg-[#171A24] rounded-xl sm:rounded-2xl p-4 sm:p-6 lg:p-8 lg:col-span-2">
  <div class="grid grid-cols-2 sm:grid-cols-4 gap-3 sm:gap-4 mb-6 lg:mb-8">
    {voices.map((voice) => (
      <div class="bg-[#2E3039] rounded-lg sm:rounded-xl px-3 sm:px-4 pt-3 sm:pt-4 flex flex-col items-center">
        <span class="text-xs sm:text-sm">{voice.alt}</span>
        <Image 
          src={voice.src} 
          alt={voice.alt}
          class="w-full max-w-[180px] lg:max-w-[180px] h-auto rounded-lg mt-6 sm:mt-8 lg:mt-10 mx-auto"
        />
      </div>
    ))}
  </div>

  <h3 class="text-2xl sm:text-2xl lg:text-3xl font-black mb-3 lg:mb-4">
    Choose the voice that matches you
  </h3>

  <p class="text-white/70 text-xs sm:text-sm font-light">
    Personalize your experience by selecting the AI voice that best suits your style. Whether it's a smooth, energetic, or professional tone, we offer a variety of options to ensure seamless communication tailored just for you.
  </p>
</div>
